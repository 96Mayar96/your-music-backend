{
  "name": "music-player-backend",
  "version": "1.0.0",
  "description": "Backend for music player using yt-dlp",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "react-scripts --max-old-space-size=8192 build",
    "postinstall": "apt-get update && apt-get install -y python3-pip && pip3 install --upgrade yt-dlp --break-system-packages",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.0.0",
    "googleapis": "^129.0.0",
    "ioredis": "^5.6.1",
    "node-cache": "^5.1.2",
    "redis": "^5.5.6"
  }
}
