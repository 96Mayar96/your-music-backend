{
  "name": "music-player-backend",
  "version": "1.0.0",
  "description": "Backend for music player using yt-dlp",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "sudo apt-get update && sudo apt-get install -y python3-pip && pip3 install --upgrade yt-dlp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "googleapis": "^129.0.0"
  }
}